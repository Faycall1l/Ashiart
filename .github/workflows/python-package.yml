name: Python Package Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.8', '3.9', '3.10', '3.11']

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi
        pip install -e .
    - name: Lint with ruff
      run: |
        python -m ruff check ashiart/ test/ examples/ scripts/
        python -m ruff format --check ashiart/ test/ examples/ scripts/
    - name: Type-check with mypy
      run: |
        python -m mypy ashiart/
    - name: Check generated API docs
      run: |
        python scripts/gen_api.py --check
    - name: Test with pytest
      run: |
        python -m pytest --cov=ashiart --cov-report=term-missing --cov-fail-under=80 